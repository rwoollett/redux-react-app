export type GeoLocation = {
  query: string;
  status: string;
  message?: string;
  city: string;
  regionName: string;
  country: string;
  countryCode: string;
  zip: string;
  lat: number;
  lon: number;
  timezone: string;
}
